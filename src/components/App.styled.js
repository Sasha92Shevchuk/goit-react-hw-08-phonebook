import styled from '@emotion/styled';

export const Wrapper = styled.div`
  margin: 20px auto 0;
  width: fit-content;
  border: 1px solid;
  border-style: dashed;
  border-radius: 5px;
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 15px;
  h1,
  h2 {
    margin: 0;
  }
`;
